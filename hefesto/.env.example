# ===========================================
# Hefesto (Frontend) — Variables de Entorno
# ===========================================
# Copia a hefesto/.env o usa el .env de la raíz del proyecto.
# Vite solo expone variables con prefijo VITE_

VITE_API_URL=http://localhost:8000/api